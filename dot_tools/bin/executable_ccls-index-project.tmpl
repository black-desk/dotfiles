#!/usr/bin/env bash

if [[ -f `pwd`/build/compile_commands.json ]]; then
    init='{"compilationDatabaseDirectory":"build","cache":{"directory":"{{ .chezmoi.homeDir }}/.ccls-cache"}}'
else
    init='{"cache":{"directory":"{{ .chezmoi.homeDir }}/.ccls-cache"}}'
fi

ccls --index=. --init=$init
