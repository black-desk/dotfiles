#!/usr/bin/env bash

if [[ $SCRIPT_DEBUG ]] ; then
  set -x
fi

if [ "$EUID" -ne 0 ]
  then echo "Please run as root"
  exit
fi

export DDE_DEBUG_MATCH="$1"
export DDE_DEBUG_LEVEL=debug
pkill -ef dde-system-daemon
/usr/lib/deepin-daemon/dde-system-daemon
